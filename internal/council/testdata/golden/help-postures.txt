  council READ  │  ~/code/telltale                                             no turn yet  │  3/3 seated  │  no brief  
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  council — what the badge on each column means                                                                         
                                                                                                                        
    Each column states its own posture; there is no room-wide claim.                                                    
                                                                                                                        
    ro:tools     the write tools are ABSENT from that session — checked                                                 
                 against what the session reported about itself, not a flag                                             
    ro:enforced  the vendor's own OS-level sandbox does it — codex, mac/linux                                           
    ro:requested a flag was passed and accepted; what it actually enforces                                              
                 was never observed. Weaker than the two above, and says so                                             
    unsandboxed  nothing restricts this vendor at the OS level. MEASURED,                                               
                 not assumed — treat this column as able to change your files                                           
    WRITES       --write or /write: this column may edit and run in the workspace                                       
    gated        --write or /write, and this seat asks first — y approves, n denies                                     
                                                                                                                        
    What contains this room is the WORKSPACE above, not any of these words.                                             
    Point council at a throwaway worktree when that matters.                                                            
    ?            close                                                                                                  
                                                                                                                        
    this room, seat by seat:                                                                                            
                                                                                                                        
    ro:tools     Claude Code                                                                                            
        this seat has no write or shell tools in its session, so it cannot edit your files. Verified by reading what t  
        session reported about itself, not by trusting a flag: the named write/exec tools are denied and MCP servers a  
        dropped. The residual is that a deny list cannot cover a tool a future release adds                             
        "tokens" — text arrives as the vendor writes it, so you are watching it land rather than waiting for it         
                                                                                                                        
    unsandboxed  Codex                                                                                                  
        nothing at the OS level stops this column reading or writing here, and that is why: on Windows both sandboxed   
        modes were measured failing every process spawn, reads included, so -s read-only was not a restriction, it was  
        seat that could not read. Council passes -s danger-full-access so this column can work at all. The workspace    
        above is the containment, not a flag — point council at a worktree if that matters                              
        "final only" — MEASURED: this vendor sends nothing at all until its turn is done, so a quiet column here is a   
        working one and not a stalled one                                                                               
                                                                                                                        
    unsandboxed  Antigravity                                                                                            
        treat this column as able to change your files, and that is MEASURED rather than assumed: asked to write a fil  
        under both --mode plan and --sandbox, it wrote the file, and its reported permission mode and tool list were    
        identical to a run without them. Council no longer passes either flag: their only observed effect was a turn    
        that died with an empty column when the agent reached for a shell. The workspace above is the containment, not  
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  ›                                                                                                                     
  VIEW                           ↑↓ scroll  │  [ ] turn  │  f expand  │  tab focus  │  i compose  │  ? help  │  q quit  